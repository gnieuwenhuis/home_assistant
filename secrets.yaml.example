# Template for the secrets referenced via !secret in configuration.yaml.
# Copy this file to secrets.yaml on the HA host (or alongside it locally)
# and fill in real values. secrets.yaml is .gitignored — never commit it.

neviweb130_username: your-neviweb-account-email
neviweb130_password: your-neviweb-account-password
